from setuptools import setup, find_packages

setup(
    name="applied_ai",
    version="0.1",
    packages=find_packages(),
    description="Modular AI libraries for RAG, generation, search, and orchestration.",
    include_package_data=True,
    install_requires=[],  # Optional, see below
)
